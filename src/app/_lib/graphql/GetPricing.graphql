query GetPricing($eventId: String!, $locale: String!) {
  event(id: $eventId, locale: $locale) {
    pricingCollection(limit: 10) {
      items {
        sys {
          id
        }
        tier
        type
        startTime
        endTime
        amount
        batch
      }
    }
  }
}
