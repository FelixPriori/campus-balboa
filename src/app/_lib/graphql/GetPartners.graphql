query GetPartners($eventId: String!, $locale: String!) {
  event(id: $eventId, locale: $locale) {
    partnersTitle
    partnersCollection(limit: 10) {
      items {
        sys {
          id
        }
        title
        link
        logo {
          title
          url
        }
      }
    }
  }
}
