query GetVenues($eventId: String!, $locale: String!) {
  event(id: $eventId, locale: $locale) {
    venuesCollection(limit: 10) {
      items {
        sys {
          id
        }
        name
        venueAddress
        purpose
      }
    }
  }
}
