query GetMissionsSection($id: String!, $locale: String!) {
  pageSection(id: $id, locale: $locale) {
    title
    anchor
    componentsCollection(limit: 10) {
      items {
        ... on Mission {
          __typename
          sys {
            id
          }
          title
          content {
            json
            links {
              assets {
                block {
                  sys {
                    id
                  }
                  url
                  description
                }
              }
            }
          }
        }
      }
    }
  }
}
